CREATE TABLE Conf (
    serverUrl TEXT NOT NULL,
    serverCertificate TEXT NOT NULL,
    clientCertificate TEXT NOT NULL,
    clientPrivateKey TEXT NOT NULL,
    lastSyncDate TEXT NOT NULL
);

insert:
INSERT
INTO Conf
VALUES ?;

selectAll:
SELECT *
FROM Conf;

deleteAll:
DELETE
FROM Conf;